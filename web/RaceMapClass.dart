part of super_racer;

class RaceMap {
  
  ImageElement background;
  List collisions;
  num startX;
  num startY;
  Map finishLine;
  
  RaceMap(this.background, this.collisions, this.startX, this.startY, this.finishLine);
  
}